export const PLAYER_RADIUS = 0.4;
export const PLAYER_HALF_HEIGHT = 1.2;

// Cylinder part of the capsule
export const PLAYER_CYLINDER_HEIGHT = PLAYER_HALF_HEIGHT * 2;

// Total capsule height
export const PLAYER_TOTAL_HEIGHT =
    PLAYER_CYLINDER_HEIGHT + PLAYER_RADIUS * 2;

export const GROUND_HALF_SIZE = {
  x: 50,
  y: 0.5,
  z: 50,
};